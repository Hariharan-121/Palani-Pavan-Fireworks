// Replace with real provider code (Ex: Twilio, MSG91)
exports.sendSMS = async (phone, message) => {
  console.log(`SMS to ${phone}: ${message}`);
  // integrate SDK here and return result
  return { success: true };
};
